package com.codexica.s3crate.filesystem

import com.codexica.s3crate.utils.SealedTraitFormat

/**
 * Represents the
 *
 * @author Josh Albrecht (joshalbrecht@gmail.com)
 */
//sealed trait PathState
//case class Deleted() extends PathState
//case class Restored() extends PathState
//case class Created() extends PathState
//case class Changed() extends PathState
//
//object PathState {
//  implicit val format = new SealedTraitFormat[PathState](Created(), Changed(), Deleted(), Restored())
//}